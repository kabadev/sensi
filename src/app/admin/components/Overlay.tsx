import React from "react";

const Overlay = () => {
	return (
		<div
			// onClick={toggleSidebar}
			className="fixed h-full w-full bg-black opacity-50 right-0 top-0 bottom-0 left-0 z-[111]"
		></div>
	);
};

export default Overlay;
